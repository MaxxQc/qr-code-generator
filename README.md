## Running the app

1. Run command `npm install` to download all dependencies
2. Open `generateQrCode.js` and edit the variables at the top of the file for the QR Code to generate.
3. Run command `npm run start`
